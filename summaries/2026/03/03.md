# Activity Summary for 3/3/2026

## 2:41:04 PM
The file `c:\ARK-Workspace\MS\microservice-notification\common\Constants.ts` was logged twice on 2/27/2026, first at 5:10:13 PM and then at 8:53:32 PM.

**File-Specific Updates:**
Both log entries for `Constants.ts` contain identical content, implying no functional changes were made between these two timestamps in the provided snippets. The content provided in both logs appears to be truncated at the same point within the `templateVideoRequestInProgress` definition.

**Content and Patterns:**
The `Constants.ts` file centralizes various constants and defines numerous email templates for a notification microservice.

1.  **DynamoDB Constants:** It defines table name (`Notification`) and primary/sort keys (`notificationId`, `time`) for a DynamoDB table.
2.  **Email Sender:** A `noreply@3rdeyecam.com` address is set as the `EMAIL_SENDER`.
3.  **Email Templates:** The bulk of the file consists of `IEmailTemplate` objects, each defining a `subjectPart`, `htmlPart`, `templateName`, and `textPart`. These templates are categorized for different event types:
    *   **General Event Templates:** `templateDecomissionEvent`, `templateSafetyEvent`, and `templateShareEvent` for asset decommissioning, safety events, and sharing events, respectively.
    *   **Pilot Product Templates:** A comprehensive set of templates for managing pilot subscriptions, targeted at the requester, approver, and customer roles. These include states like `Submitted`, `Approved`, `Denied`, `Started`, `ExpiringSoon`, and `Expired`. A common `VEHICLE` constant is used across these templates to consistently identify the asset (`"Vehicle: {{tenantName}} / {{divisionName}} / {{assetName}}"`).
    *   **Video Request Templates:** `templateVideoRequestReview` and `templateVideoRequestInProgress` handle the workflow for video pull requests, detailing requester information, vehicle, and video specifics.
4.  **Placeholders:** All email templates heavily utilize placeholders (e.g., `{{assetId}}`, `{{recipient}}`, `{{productIdsHtml}}`, `{{startDateSimple}}`, `{{customerName}}`) which suggests a templating engine is used to dynamically populate email content.
5.  **HTML and Text Parts:** Each template provides both an HTML version (using standard HTML tags like `<p>`, `<br>`, `<ul>`) and a plain text version (using `\n` for line breaks), ensuring broad compatibility for email clients.
6.  **Recurring Elements:** The pattern of defining constants and structured email templates with placeholders is consistent throughout the file, indicating a standardized approach to notification content generation within the microservice.