# Activity Summary for 3/2/2026

## 2:24:16 PM
The log details changes within the `c:\ARK-Workspace\MS\microservice-notification\common\Constants.ts` file, a central repository for constants in a microservice notification system.

**File-specific updates and content:**

The `Constants.ts` file is primarily concerned with defining various constants for application configuration and a comprehensive set of email templates.

*   **DynamoDB Configuration:** It establishes constants for a `Notification` DynamoDB table, specifying `NOTIFICATIONS_TABLE_NAME` as "Notification", `NOTIFICATIONS_PK` as "notificationId", and `NOTIFICATIONS_SK` as "time".
*   **Email Sender:** A `EMAIL_SENDER` constant is defined as "noreply@3rdeyecam.com".
*   **Email Templates (IEmailTemplate):** The bulk of the file comprises definitions of numerous email templates, each conforming to the `IEmailTemplate` interface (imported from `./Interfaces`). These templates include a `subjectPart`, `htmlPart`, `templateName`, and `textPart`.
    *   **Event-based Templates:** Templates are defined for general events such as `templateDecomissionEvent` (asset decommissioning), `templateSafetyEvent` (safety event occurrence), and `templateShareEvent` (event sharing).
    *   **Pilot Product Lifecycle Templates:** A significant portion is dedicated to pilot product subscriptions, categorized by the recipient:
        *   **Requester-facing:** `templatePilotRequesterSubmitted`, `templatePilotRequesterApproved`, `templatePilotRequesterDenied`, `templatePilotRequesterStarted`, `templatePilotRequesterExpiringSoon`, and `templatePilotRequesterExpired`. These guide the requester through the pilot process.
        *   **Approver-facing:** `templatePilotApproverSubmitted` for approval requests.
        *   **Customer-facing:** `templatePilotCustomerStarted`, `templatePilotCustomerExpiringSoon`, and `templatePilotCustomerExpired` to inform customers about pilot status.
    *   **Video Request Templates:** Templates for managing video requests, including `templateVideoRequestReview` (request received and under review) and `templateVideoRequestInProgress` (video pull initiated). The definition of `templateVideoRequestInProgress` is truncated at the end in both log entries.

**Timestamps of significant changes:**

The initial log entry at **2/27/2026, 5:10:13 PM** contains all the detailed constant and email template definitions. The subsequent log entry at **2/27/2026, 8:53:32 PM** shows no discernible changes to the file's content; it is an exact duplicate of the earlier timestamp's content, suggesting either no changes were made between these times or the log captured the same state twice.

**Patterns or recurring elements in the content:**

*   **Standardized Template Structure:** All email templates consistently adhere to the `IEmailTemplate` structure, providing distinct HTML and plain text versions along with a subject and a unique template name.
*   **Placeholder Usage:** Extensive use of `{{placeholderName}}` syntax throughout the subject and body parts (HTML and text) of the templates. This indicates a templating engine is used to dynamically insert data such as asset IDs, recipient names, vehicle details, product IDs, dates, and contact information.
*   **`VEHICLE` Constant:** The `VEHICLE` constant (`"Vehicle: {{tenantName}} / {{divisionName}} / {{assetName}}"`) is frequently reused across the pilot product email templates, ensuring consistency in how vehicle information is presented.
*   **Logical Grouping:** Templates are logically grouped by their functional area (e.g., general events, pilot product lifecycle, video requests), enhancing readability and maintainability.

## 2:24:19 PM
The `c:\ARK-Workspace\MS\microservice-safety\lambda\safetyReview\index.ts` file, updated on 2/27/2026 at 4:56:49 PM, outlines a core Lambda function, `safetyReviewHandler`, responsible for processing safety events within a microservice. This handler performs several critical steps:
*   It retrieves a `SafetyEvent` from DynamoDB using a `correlationId`.
*   It transforms `multiLabel` camera data within the safety event's media to a `no-multiLabel` format for review purposes.
*   It fetches asset details and associated use cases by making internal API calls to an `Asset` microservice (using a `PRIVATE_API` endpoint derived from an environment variable).
*   It then selects relevant use cases via another internal API call to an `AI Admin` service.
*   Crucially, if no media is present or no matching use cases are found, the process is halted with an error.
*   Finally, it constructs an `EventReviewRequest` and publishes it to an AWS EventBridge event bus (`EVENT_BUS`) using the `REVIEW_EVENT_PUBLISH` source.
*   The safety event's status in DynamoDB is updated to `PendingReview` and `Pending`.
*   The file also contains helper functions like `getAssetById`, `getAssetUsecases`, and `selectUseCases`, which handle the specifics of interacting with other internal APIs. Error handling and extensive logging are present throughout the code.

The `c:\ARK-Workspace\MS\microservice-safety\lib\policies.ts` file, updated shortly after on 2/27/2026 at 4:57:44 PM, defines several AWS IAM Policy Statements using AWS CDK. These policies grant necessary permissions for different AWS services:
*   `createEventBusPolicy`: Allows putting and getting events from an EventBridge bus.
*   `createEventStreamReadPolicy`: Provides read access to Kinesis Data Streams.
*   `createEventStreamDecryptPolicy`: Grants KMS decryption permissions for an event stream's encryption key.
*   `createStateMachinePolicy`: Allows interacting with AWS Step Functions, specifically sending task success/failure and starting executions.
Each function consistently returns an `Effect.ALLOW` policy statement for specific actions on a given resource ARN.

**Key Information and Patterns:**
The changes highlight an event-driven microservice architecture with strong integration with AWS services. The `safetyReview` Lambda acts as an orchestrator, retrieving event data, enriching it by calling other internal microservices (for asset and use case information), performing data transformations, and then publishing a new event to EventBridge for further processing. There's a clear emphasis on observability through detailed logging. The related policy definitions indicate that these components are managed via Infrastructure as Code (likely AWS CDK), ensuring proper permissions for service interactions. The timestamps are very close, suggesting a consolidated update or deployment around the same time.

## 2:24:25 PM
The provided logs detail changes across several TypeScript files within a `microservice-coaching` project, primarily focusing on the AWS Cloud Development Kit (CDK) stack definitions for storage and API, and the backend logic for a coaching session download workflow.

**File-Specific Updates and Timestamps:**

**1. `c:\ARK-Workspace\MS\microservice-coaching\lib\coachingApiStorageStack.ts`**
   - **Timestamp: 2/27/2026, 5:48:23 PM**: Initial creation or a significant update to the CDK stack defining storage resources. It establishes a DynamoDB table named "Coaching" with `tenantId` as partition key and `coachingId` as sort key, configured for on-demand billing, DynamoDB streams, point-in-time recovery, and integration with an optional Kinesis BI stream. It also sets up three Global Secondary Indexes (GSIs) on `driverId`, `completionDate`, and `coachId`.
   - **Timestamp: 2/27/2026, 8:01:48 PM - 2/27/2026, 8:06:21 PM**: Multiple, minor updates within this timeframe focusing on the `createCoachingDownloadBucket` function.
     - The S3 bucket policy for `S3_COACHING_DOWNLOAD_BUCKET` was modified to enforce SSL. Initially, a single policy statement denied `s3:PutObject`, `s3:GetObject`, `s3:ListBucket`, and `s3:DeleteObject` actions if `aws:SecureTransport` was false, applying to `arnForObjects("*")`.
     - This was then refined into two distinct policy statements: one for object-level actions (`s3:PutObject`, `s3:GetObject`, `s3:DeleteObject`) on `arnForObjects("*")` and another for bucket-level actions (`s3:ListBucket`) on `bucketArn`, both denying non-SSL access.
     - Finally, these two separate policies were consolidated back into a single policy statement denying all `s3:*` actions for non-SSL requests on `arnForObjects("*")`. The core intent remains enforcing SSL for S3 bucket access.

**2. `c:\ARK-Workspace\MS\microservice-coaching\lib\coachingApiStack.ts`**
   - **Timestamp: 2/27/2026, 7:34:59 PM**: This file defines the main API stack for the coaching service. Key elements include:
     - Importing configuration values (API ID, VPC name, Lambda paths, workflow constants) from `@common/consts`.
     - Importing the `CoachingApiStorageStack` to utilize its DynamoDB table and S3 bucket.
     - Creating several AWS Lambda functions for core API operations (`CoachingLambdaFn`, `CoachingEventLambdaFn`) and a detailed download workflow (`CoachingDownloadInitializerLambdaFn`, `CoachingDataFetcherLambdaFn`, `GetSafetyForCoachingId`, `CoachNameFetcherLambdaFn`, `DriverNameFetcherLambdaFn`, `CoachingCsvGeneratorLambdaFn`, `ConsolidatorCoachingCsvLambdaFn`, `CoachingDownloadRequestLambdaFn`).
     - Configuring `CoachingEventLambdaFn` to process DynamoDB stream events from the `coachingTable`.
     - Setting up a Step Functions State Machine (`COACHING_DOWNLOAD_STATE_MACHINE`) for the download workflow, integrating all the download-related Lambda functions.
     - Deploying the Coaching API using `UpdateApiPattern` and linking it to the `coachingLambdaFn` and `downloadCoachingRequestLambdaFn`.
     - Adding permissions and environment variables (e.g., `COACHING_DOWNLOAD_STATE_MACHINE_ARN`) to Lambdas.
     - Including CDK Nag suppressions for common issues, particularly related to IAM and Step Functions logging.

**3. `c:\ARK-Workspace\MS\microservice-coaching\src\download-workflow\ConsolidatorCoachingCsvLambdaFn.ts`**
   - **Timestamp: 2/27/2026, 8:19:59 PM - 2/27/2026, 8:22:56 PM**: Minor formatting changes or re-saves without functional differences. This Lambda's handler consolidates batched CSV files from S3, generates a final CSV, uploads it, creates a presigned URL, sends completion notifications via EventBridge, and cleans up workflow files. It handles cases where no CSV data is found by sending a failure notification.
   - **Timestamp: 2/27/2026, 8:36:36 PM**: No functional change observed.
   - **Timestamp: 2/27/2026, 9:01:21 PM - 2/27/2026, 9:14:01 PM**: The import for `EventBridgeClient` and `PutEventsCommand` was removed, and the `eventBridgeClient` instance was deleted. This change effectively disabled EventBridge notifications directly from this Lambda. However, the `sendCoachingCompletionNotification` and `sendCoachingFailureNotification` functions still attempt to use `eventBridgeClient`, leading to a potential error (or requiring these functions to be moved or re-implemented).
   - **Timestamp: 3/2/2026, 10:55:38 AM - 3/2/2026, 12:16:01 PM**: A series of changes related to EventBridge integration.
     - The `EventBridgeClient` and `PutEventsCommand` imports were re-added.
     - The `sendCoachingCompletionNotification` and `sendCoachingFailureNotification` functions were removed from this file, implying they were either moved to `CoachingDownloadBackend.ts` or made available globally.
     - The `generatePresignedUrl` function call was updated to pass `workflowId` as a second argument.
     - The `sendCoachingCompletionNotification` and `sendCoachingFailureNotification` functions were re-added to this file, suggesting a temporary removal/re-addition or a refactoring attempt. The `eventBridgeClient` dependency indicates that this Lambda is once again responsible for sending these notifications.
   - **Timestamp: 3/2/2026, 12:16:01 PM - 3/2/2026, 12:14:41 PM**: No functional changes are observed in these minor updates, indicating re-saves or formatting adjustments.

**4. `c:\ARK-Workspace\MS\microservice-coaching\src\api-coaching\CoachingDownloadRequestLambdaFn.ts`**
   - **Timestamp: 2/27/2026, 8:24:10 PM - 2/27/2026, 8:36:28 PM**: Minor re-saves/formatting. This Lambda handles API Gateway requests for coaching session downloads. It validates input, de-duplicates `coachingIds`, stores them to S3, and initiates a Step Functions workflow for the actual download process. It includes robust error logging and EventBridge notifications for workflow failures.
     - A notable change at **2/27/2026, 8:36:28 PM** was a modification in `sendWorkflowFailureNotification`. The `DetailType` for the EventBridge event was changed from "CoachingDataDownloadFailed" to "CoachingDataDownloadCompleted", which seems like a logical error as it's intended for failure notifications.
   - **Timestamp: 3/2/2026, 11:00:38 AM**: An important change was made: the `sendCoachingFailureNotification` function was imported from `download-workflow/CoachingDownloadBackend` and the `eventBridgeClient` instance in this file was removed, implying that notification logic is centralized in the backend utility. The `DetailType` within `sendWorkflowFailureNotification` was correctly reverted to `DOWNLOAD_COACHING_DETAIL_TYPE` (which is defined as "CoachingDataDownloadCompleted" in `consts.ts` at 9:00:26 PM and 9:01:21 PM). This still looks like a mismatch for a *failure* notification detail type.
   - **Timestamp: 3/2/2026, 11:00:53 AM - 3/2/2026, 12:06:34 PM**: No functional changes; likely re-saves. The `sendWorkflowFailureNotification` function is also re-defined *within* this file at 12:06:34 PM, re-introducing `eventBridgeClient` and duplicating functionality with `CoachingDownloadBackend.ts`. The `DetailType` for failure remains `DOWNLOAD_COACHING_DETAIL_TYPE` ("CoachingDataDownloadCompleted").
   - **Timestamp: 3/2/2026, 12:07:31 PM**: No functional change observed.

**5. `c:\ARK-Workspace\MS\microservice-coaching\src\common\consts.ts`**
   - **Timestamp: 2/27/2026, 8:59:52 PM**: Defines various constants for the microservice, including:
     - AWS CloudFormation export keys for private API.
     - Lambda function names, descriptions, and paths for API and download workflow components.
     - Step Functions state machine name and description.
     - DynamoDB table and column names (`Coaching`, `tenantId`, `coachingId`).
     - Error messages and API resource paths.
     - Input parameters for queries.
     - DynamoDB update expressions and attribute names.
     - DynamoDB index names (`CoachingIndex-DriverId`, `CoachingIndex-CompletionDate`, `CoachingIndex-CoachId`).
     - EventBridge constants (`EVENT_BUS_NAME`, `EVENT_LAMBDA_EVENTBUS_ACTIONS`, `COACHING_DATE_RESOURCE`, `DOWNLOAD_COACHING_RESOURCE`).
     - S3 bucket name (`coaching-downloads`).
     - Workflow constants (`WORKFLOW_BATCH_SIZE`, `WORKFLOW_MAX_PARALLEL_EXECUTIONS`, `WORKFLOW_TIMEOUT_MINUTES`).
   - **Timestamp: 2/27/2026, 9:00:26 PM**: Added `DOWNLOAD_COACHING_DETAIL_TYPE = "CoachingDownloadWorkflowCompleted"` and removed `COACHING_DATE_UPDATE_DETAIL_TYPE`. This indicates a renaming/clarification of EventBridge detail types related to coaching download workflows.

**6. `c:\ARK-Workspace\MS\microservice-coaching\src\common\CoachingDownloadInterfaces.ts`**
   - **Timestamp: 3/2/2026, 9:46:48 AM**: Defines TypeScript interfaces crucial for the coaching download workflow, including:
     - `WorkflowLambdas`: Specifies the types for all Lambda functions involved in the workflow.
     - `BaseInitializer`, `CoachingDownloadInitializerEvent`, `CoachingDataFetcherEvent`, `CoachingDataFetcherResponse`: For event passing and data structures within the workflow.
     - `CoachingData`: Represents core coaching session data.
     - `SafetyEvent`, `InfractionDetail`, `ReviewResult`: For safety event data and infraction details.
     - `CoachingCsvGeneratorEvent`: Defines the input structure for the CSV generator Lambda.
     - `PassCoachingData`, `SafetyBranchResponse`, `CoachNameBranchResponse`, `DriverBranchResponse`: For intermediate data passed between workflow steps.
     - `UserResponse`, `DriverResponse`: Interfaces for user and driver details fetched from external APIs.
     - `CombinedCoachingData`: Represents the merged data of coaching and safety events for CSV generation.

**7. `c:\ARK-Workspace\MS\microservice-coaching\src\download-workflow\CoachingDownloadBackend.ts`**
   - **Timestamp: 2/28/2026, 12:07:50 AM - 2/28/2026, 12:10:35 AM**: Initial implementation of backend utility functions for the coaching download workflow. This includes:
     - `batchGetCoachingDetails`: Fetches coaching data from DynamoDB in batches.
     - `getSafetyEventsForBatch`: Fetches safety event data from a private API.
     - `fetchDriverDetails`, `fetchUserDetails`: Fetches driver and user details from external private APIs.
     - `generateCsv`: Combines coaching data and safety events to generate a CSV string.
     - `generatePresignedUrl`: Creates a presigned S3 URL for downloading the generated CSV.
     - `cleanupWorkflowFiles`: Deletes temporary files from S3.
     - A significant change in `generateCsv` at 12:08:50 AM (and subsequent timestamps) simplifies the `combinedData` generation. Instead of iterating through coaching rows and then safety events, it maps coaching data directly, using `safetyEventMap.get(coachingData.coachingId)` which implicitly means one coaching session maps to at most one safety event, potentially losing multiple safety events if they exist for a single coachingId.
   - **Timestamp: 3/2/2026, 9:50:16 AM**: Introduced a new helper function `mergeCoachingWithOptionalSafety` which correctly handles cases where a coaching session might have multiple safety events or no safety events. The `generateCsv` function was updated to use this new merge logic. This addresses the potential data loss issue from previous versions.
   - **Timestamp: 3/2/2026, 10:54:55 AM**: EventBridge clients and associated constants were imported, and `sendCoachingCompletionNotification` and `sendCoachingFailureNotification` functions were added to this utility file. This centralizes the EventBridge notification logic for the download workflow.
   - **Timestamp: 3/2/2026, 10:55:30 AM - 3/2/2026, 12:21:21 PM**: Various minor re-saves and formatting updates.
   - **Timestamp: 3/2/2026, 12:21:10 PM**: The `generatePresignedUrl` function was updated to accept `workflowId` as a parameter, which is then used in the `ResponseContentDisposition` header for the filename.

**Patterns and Recurring Elements:**

1.  **AWS CDK for Infrastructure as Code**: `coachingApiStorageStack.ts` and `coachingApiStack.ts` consistently use AWS CDK (TypeScript) to define, provision, and manage AWS resources like DynamoDB tables, S3 buckets, Lambda functions, and Step Functions state machines.
2.  **Microservice Architecture**: The project is structured as a microservice ("microservice-coaching") with distinct API and event-driven components.
3.  **AWS Lambda Functions**: A core pattern is the extensive use of Node.js Lambda functions (`NodejsFunction` in CDK) for various operations, including API handling, event processing, and all steps of the download workflow.
4.  **DynamoDB as Primary Data Store**: The `Coaching` DynamoDB table is central, with multiple Global Secondary Indexes for efficient querying by `driverId`, `completionDate`, and `coachId`.
5.  **S3 for File Storage**: An S3 bucket (`coaching-downloads`) is dedicated to storing exported coaching data and temporary workflow files, with lifecycle rules for cleanup and enforced SSL.
6.  **Step Functions for Workflow Orchestration**: A `CoachingDownloadStateMachine` orchestrates a complex download process involving multiple Lambda functions, demonstrating a pattern for managing multi-step business logic.
7.  **Event-Driven Architecture with EventBridge**: EventBridge is used for sending notifications about the completion or failure of coaching data downloads, allowing for decoupled communication within the system.
8.  **X-Ray for Distributed Tracing**: `AWSXRay.captureAWSv3Client` is consistently used to instrument AWS SDK clients (S3, SFN, EventBridge, DynamoDB) for X-Ray tracing, indicating a focus on observability.
9.  **Centralized Constants**: The `@common/consts.ts` file centralizes literal strings for resource names, paths, error messages, and API endpoints, promoting consistency and maintainability.
10. **Robust Error Handling and Logging**: All Lambda functions and backend utilities include comprehensive `try-catch` blocks and use `@aws-lambda-powertools/logger` for detailed logging, enhancing debuggability. EventBridge is used to propagate failure notifications.
11. **API Integration**: Lambdas interact with external "private APIs" (e.g., `/safety-search`, `/drivers/{id}`, `/users/{id}`) to enrich coaching data with safety events, driver, and user details.
12. **CSV Generation**: A specific workflow is dedicated to generating CSV reports by merging data from DynamoDB and external APIs.
13. **SSL Enforcement**: Security is addressed by explicitly enforcing SSL for S3 bucket access through resource policies.
14. **CDK Nag Suppressions**: The use of `NagSuppressions` indicates adherence to security best practices and a mechanism to manage exceptions to common AWS Solutions security rules.

The most significant functional changes observed are the evolution of the S3 bucket policy for SSL enforcement, the refactoring and centralization of EventBridge notification logic into `CoachingDownloadBackend.ts`, and the correction of CSV generation logic in `CoachingDownloadBackend.ts` to correctly handle multiple safety events per coaching session by introducing `mergeCoachingWithOptionalSafety`. There were also temporary inconsistencies in EventBridge notification handling in `CoachingDownloadRequestLambdaFn.ts` that were later resolved or clarified.