# Activity Summary for 5/29/2026

## 1:25:37 PM
The file `c:\ARK-Workspace\UI\ui-tenant\src\app\core\interceptors\app.interceptor.ts`, last updated on 5/29/2026 at 12:25:45 PM, defines an `AppInterceptor` for Angular applications.

**Key Updates and Functionality:**

*   **Authentication and Token Management:** The interceptor integrates with `aws-amplify/auth` to `fetchAuthSession`, proactively refreshing authentication tokens if they exist and are not expired. It then sets an `Authorization` header with a `Bearer` token for most outgoing HTTP requests, skipping this for external URLs or if no token is available.
*   **Dynamic Base URL Resolution:** It includes utility functions (`getBaseUrl`, `buildBaseUrl`) to determine the API base URL. This base URL is retrieved from `localStorage` if available, or constructed based on the application's `origin` (e.g., defaulting to `https://api.stage.ark.3rdeyecam.com/` for localhost, or transforming `eyesite` domains to `api` domains). This ensures relative API calls are correctly routed.
*   **Comprehensive Error Handling:** The interceptor provides detailed error handling for various HTTP status codes:
    *   **401 (Unauthorized):** Displays "Session expired" and triggers a sign-in process.
    *   **403 (Forbidden):** Notifies the user about lacking permission to access data.
    *   **404 (Not Found):**
        *   Includes specific handling for `/mostImprovedDriverOfTheWeek` and `/worstDriverOfTheWeek` URLs, where it emits a message via dedicated `Subject`s (`mostImprovedDriverMessage$`, `riskiestDriverMessage$`) and returns a `HttpResponse` with a null body. This prevents a general "Resource not found" snackbar for these specific cases, allowing the application to display custom messages.
        *   For other 404s, it shows a generic "Resource not found" snackbar.
    *   **409 (Conflict):** Displays a conflict message, potentially extracted from the error response.
    *   **Default Error Handling:** Attempts to parse complex error objects, including deeply nested JSON strings, to extract a user-friendly error message, defaulting to "An error occurred. Please try again later."
*   **User Feedback:** The `MatSnackBar` service is extensively used across all error handling paths to provide immediate visual feedback to the user.
*   **Environment Detection:** A `getArkEnv` utility function is present to extract the environment (e.g., "stage", "dev", "prod") from the hostname, defaulting to "stage" for `localhost`.

**Patterns and Recurring Elements:**

*   **Extensive use of `MatSnackBar`** for displaying various informational and error messages to the user.
*   **Conditional URL handling** is present for prepending the base URL and for specific error handling based on the request URL.
*   **Dependency on an `AuthService`** for managing authentication state and actions (like signing in).
*   **Complex error object parsing** is a recurring theme in the default error handling, indicating a need to extract meaningful messages from varied backend error formats.
*   **RxJS operators** like `catchError`, `switchMap`, `from`, and `of` are integral to handling the asynchronous nature of HTTP requests and error propagation.