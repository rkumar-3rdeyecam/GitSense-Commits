# Activity Summary for 10/29/2025

## 1:18:06 PM
The `c:\ARK-Workspace\MS\microservice-safety\lib\consts.ts` file, last updated at **10/29/2025, 12:58:58 PM**, serves as a centralized repository for all constants within the microservice. It defines a wide array of configurations, including:
*   **DynamoDB Schema**: Partition and sort keys, table name (`SafetyEvent`), Global Secondary Index (GSI) names (`assetGSI`), and numerous index definitions for querying by driver ID, asset ID, coaching ID, event status, tenant ID, division ID, address, epoch time, and combinations thereof.
*   **Data Model Attributes**: Specific column names for DynamoDB items (e.g., `driverId`, `tenantIdDivisionId`, `epochTimeOfEvent`).
*   **Event Types**: A comprehensive list of `SAFETY_EVENT_SUBTYPES` (e.g., seat-belt-notbuckled, speed, hard-brake) and `VEHICLE_EVENT_SUBTYPES` (e.g., vehicle-idle-start, live-tampering), which are combined into `EVENT_SUBTYPES`.
*   **Identifier Prefixes**: Standardized prefixes for various IDs like `D#` for Division/Driver ID, `T#` for Tenant ID, `C#` for Coaching ID, `A#` for Asset ID, and `E#` for Event Status.
*   **API Endpoints**: Defines routes for interacting with safety events, such as `/safety/asset`, `/safety/{eventId}`, and `/safety/{eventId}/updateCoachingInfo`.
*   **System Messages**: A collection of specific error messages for various failure scenarios (e.g., `ERROR_UNSUPPORTED_OPERATION`, `ERROR_BACKEND_FAILURE`).
*   **AWS Service Configuration**: Constants for EventBridge bus names (`ArkEventBus`, `default`), review statuses (`New`, `Requested`, `Performed`, `NotPerformed`), and event names for EventBridge publication (`safetyEventUpdated`, `safetyEventScoreUpdated`, `safetyEventPublish`).
*   **Lambda Function Definitions**: Names, script paths, and permission names for numerous Lambda functions that handle different aspects of the microservice, including driver lookup, safety review, review timeouts, error handling, points calculation, event processing, OpenSearch publishing, infraction score configuration, status updates, API interactions, event sharing, and metrics publishing.
*   **Step Functions State Machine**: Defines the name for the state machine (`safetyStateMachine`) and various timeouts (`REVIEW_TIMEOUT`, `BRIEF_WAIT`, `STATE_MACHINE_TIMEOUT`).
*   **API Parameters and DynamoDB Filter Expressions**: Specifies parameters for API requests (`coachingId`, `includeCoachingEvents`) and a wide range of DynamoDB filter expressions to query the `SafetyEvent` table.

The `c:\ARK-Workspace\MS\microservice-safety\lib\safety-service-stack.ts` file details the deployment of the microservice's AWS infrastructure, primarily Lambda functions and a Step Functions state machine, leveraging CDK patterns. Significant changes occurred between **10/29/2025, 1:05:11 PM** and **10/29/2025, 1:15:03 PM**.

**Summary of `safety-service-stack.ts` changes:**

*   **Initial Configuration (10/29/2025, 1:05:11 PM)**: This version established the core CDK stack. It imports necessary AWS constructs and custom CDK patterns. It retrieves configuration from SSM Parameter Store (e.g., VPC, API, Event Bus details) and sets up a `LambdaSecurityGroup`. It defines multiple Lambda functions (`safetyLookupDriver`, `safetyReview`, `safetyReviewTimeout`, `safetyReviewError`, `safetyPoints`, `safetyEvent`, `safetyOpenSearchPublish`, `safetyTask`) using `CrudLambdaPattern` or `EventLambdaPattern`, connecting them to DynamoDB and Kinesis streams where appropriate. A Step Functions state machine (`safetyStateMachine`) is defined, orchestrating `driverLookupTask`, a `wait` step, `reviewTask`, and `pointsTask`. The `reviewTask` is configured to wait for a task token and includes an error catch for `States.Timeout` which triggers `reviewTaskTimeOUt`. However, a catch for general errors was present but referred to an undefined variable `reviewTaskError`.
*   **Error Handling Refinement (10/29/2025, 1:11:42 PM)**: This revision added a comment clarifying the state machine's success path and formally introduced an `addCatch` block for the `reviewTask` to handle a broader range of errors (`Lambda.ServiceException`, `States.TaskFailed`, `States.ALL`), but `reviewTaskError` was still undeclared before its usage in this catch block.
*   **Robust Error Integration (10/29/2025, 1:13:04 PM)**: This update successfully integrated the `safetyReviewErrorLambdaPattern` into the state machine's error handling. A new `LambdaInvoke` task, `reviewTaskError`, was explicitly defined and chained to the `commonCompletion` state. This `reviewTaskError` task, using the `safetyReviewErrorLambdaPattern` lambda, is now correctly specified as the target for non-timeout errors caught from the `reviewTask`. This marks a significant improvement in the state machine's fault tolerance.
*   **Minor Anomaly (10/29/2025, 1:15:03 PM)**: A very minor change was introduced in the `reviewTaskError` definition, adding a "0" (`outputPath: "$", 0}`). This appears to be a syntax error or a partial edit.

**Patterns and Recurring Elements:**

*   **Centralized Configuration**: All key parameters and static strings are managed in `consts.ts`, demonstrating a strong pattern of configuration externalization.
*   **CDK Patterns Usage**: Extensive use of `@cloud-arkdev/cdk-patterns` (`CrudLambdaPattern`, `EventLambdaPattern`) for consistent and streamlined Lambda deployments, including common configurations like VPC integration, table permissions, and environment variables.
*   **Event-Driven Architecture**: The service heavily relies on AWS EventBridge and Kinesis/DynamoDB Streams to trigger Lambda functions and facilitate communication between components.
*   **Step Functions Orchestration**: A core business logic flow is orchestrated via AWS Step Functions, defining a clear sequence of tasks (`driverLookup`, `review`, `points`) with explicit `wait` steps and robust error handling.
*   **Detailed Logging and Monitoring**: Lambdas are configured for JSON logging, debug log levels, and active tracing, and CloudWatch alarms are created for critical functions, indicating a focus on observability.
*   **Consistent Lambda Configuration**: Many Lambda functions are configured with a `PVT_API_URL` environment variable, VPC integration, `readwrite` permissions to the safety table, and specific memory sizes (e.g., 512MB or 1024MB), maintaining uniformity across the service.
*   **Incremental Refinement**: The timestamped changes in `safety-service-stack.ts` show a pattern of iterative development, specifically focusing on enhancing error handling within the Step Functions state machine to make it more resilient.