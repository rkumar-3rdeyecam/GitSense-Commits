# Activity Summary for 9/20/2025

## 12:59:04 AM
The commit on September 20, 2025, at 12:43:40 AM, modifies `SearchApiBackend.ts`.  The primary change is an update to the `listSearchItemsOS` function, which retrieves search results from an OpenSearch collection. This function now incorporates pagination using `search_after` for improved performance with large datasets.  The pagination logic handles sorting by various fields (`sortField`), including special handling for `null` and numerical values.

The `buildOpenSearchQuery` function, also updated, constructs OpenSearch queries based on various search criteria including `tenantId`, `divisionIds`, `driverId`, `assetId`, `eventName`, `eventTypes`, `eventStatus`, `reviewStatus`, `coachId`, `infractionScore`, `coachingId`, `metric`, `startDate`, `endDate`, and pagination parameters.  The function meticulously handles different data types and optional parameters, building complex boolean queries with `term`, `terms`, `bool`, and `exists` clauses.  Noteworthy is the special handling of `driverId` (allowing "null" and "notnull" searches), `eventTypes` (handling comma-separated lists), and date range filtering.  The function also includes a tie-breaker in the sorting mechanism using `_id`.
