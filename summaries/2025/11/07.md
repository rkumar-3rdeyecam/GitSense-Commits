# Activity Summary for 11/7/2025

## 10:17:38 AM
The `c:\ARK-Workspace\MS\microservice-safety\lib\consts.ts` file, last modified on 11/3/2025 at 4:26:00 PM, serves as a central repository for constants within the `microservice-safety` project.

**Key Information from Changes:**

*   **Core DynamoDB Table Structure**: Defines the primary `SafetyEvent` table with `SAFETY_PARTITION_KEY` as "tenantIdDivisionId" and `SAFETY_SORT_KEY` as "correlationId". It also establishes a Global Secondary Index (`SAFETY_ASSET_GSI`) on "assetId".
*   **Timeouts and Durations**: Constants using `aws-cdk-lib`'s `Duration` are defined for `REVIEW_TIMEOUT` (5 minutes), `BRIEF_WAIT` (5 seconds), and `STATE_MACHINE_TIMEOUT` (8 minutes).
*   **Safety Event Categories**: A comprehensive list of `SAFETY_EVENT_SUBTYPES` covers various incidents like "seat-belt-notbuckled," "speed," "hard-brake," "forward-collision-warning-active," and "roll-over." `VEHICLE_EVENT_SUBTYPES` include "vehicle-idle-start/stop" and "live-tampering," which are combined into a master `EVENT_SUBTYPES` list.
*   **Identifier Prefixes**: Standardized prefixes (`PREFIX_DIVISION_ID`, `PREFIX_TENANT_ID`, `PREFIX_ASSET_ID`, etc.) are established for generating unique identifiers.
*   **API Resources**: Specific API endpoint paths related to safety events, assets, drivers, and coaching information are defined (e.g., `/safety/asset`, `/safety/{eventId}/updateCoachingInfo`).
*   **Error Messages**: A collection of `ERROR_` constants provides standardized messages for unsupported operations, backend failures, and issues during event retrieval or updates.
*   **AWS Lambda Functions**: A substantial portion of the file is dedicated to defining constants for numerous AWS Lambda functions, specifying their names, script paths (e.g., `lambda/safetyLookupDriver/index.ts`), and associated permissions. This includes functions for driver lookup, API handling, safety event review, review timeouts, error handling, points calculation, state machine orchestration, OpenSearch publishing, infraction score configuration, status updates, event sharing, and metrics publishing.
*   **Event-Driven Architecture**: Constants for AWS EventBridge buses (`ArkEventBus`, `default`) and various event names are defined (`com.ark.review.requested`, `safetyEventUpdated`, `safetyEventScoreUpdated`, `eventSharePublish`, `eventMetricsPublish`). This highlights an event-driven microservice design.
*   **Review Workflow Statuses**: Enumerated states for event review are provided: "New", "Requested", "Performed", "NotPerformed".
*   **DynamoDB Indexes and Columns**: An extensive set of constants defines numerous Global Secondary Indexes (`SAFETY_EVENT_DB_INDEX_`) to support diverse query patterns on `SafetyEvent` data, often combining `tenantId`, `driverId`, `assetId`, `eventStatus`, `coachingId`, `epochTimeOfEvent`, `divisionId`, `eventName`, `videoRequestId`, and `reviewStatus`. Corresponding column names (`SAFETY_EVENT_DB_COL_`) are also listed.
*   **API Parameters and Filter Expressions**: Constants for API request parameters (e.g., `API_PARAM_COACHING_ID`) and DynamoDB filter expressions (e.g., `SAFETY_EVENT_DB_FILTER_DRIVER_ID`, `SAFETY_EVENT_DB_FILTER_TIME_OF_EVENT`) are defined, indicating common data access patterns.

**Patterns and Recurring Elements:**

*   **Naming Conventions**: A strong pattern of prefixing constants (`SAFETY_`, `PREFIX_`, `ERROR_`, `LAMBDA_`, `REVIEW_`, `EVENT_BUS_`, `DDB_STREAM_EVENT_SOURCE`, `SAFETY_EVENT_DB_INDEX_`, `SAFETY_EVENT_DB_COL_`, `API_PARAM_`, `SAFETY_EVENT_DB_FILTER_`) is consistently used to categorize and organize related values.
*   **AWS Service Integration**: The code heavily relies on AWS services, defining explicit constants for DynamoDB tables, indexes, attributes, Lambda functions (names, scripts, permissions), and EventBridge event buses and event types.
*   **Microservice Component Definition**: The extensive enumeration of Lambda functions, API paths, and event types clearly outlines the individual functional components and their interconnections within the `microservice-safety` system.
*   **Safety Event Lifecycle**: The constants collectively describe a comprehensive lifecycle for safety events, from various subtypes and initial creation to review, coaching, status updates, scoring, and metrics publication.
*   **Data Query Flexibility**: The large number of DynamoDB indexes and filter expressions indicates a critical requirement for flexible and efficient querying of safety event data across multiple dimensions (driver, asset, tenant, time, status, etc.).

## 11:17:29 AM
The `c:\ARK-Workspace\MS\microservice-safety\lib\consts.ts` file, last updated on 11/3/2025, 4:26:00 PM, serves as a central repository for constants within a microservice focused on "safety events."

Key information from this file includes:

*   **Core Data Model and Database Configuration**: Defines the primary keys (`SAFETY_PARTITION_KEY`, `SAFETY_SORT_KEY`), table name (`TABLE_SAFETY`), and a Global Secondary Index (`SAFETY_ASSET_GSI`) for a `SafetyEvent` DynamoDB table. An extensive list of over 20 `SAFETY_EVENT_DB_INDEX_...` constants outlines various DynamoDB Global Secondary Indexes, many combining `TenantId` with other attributes like `DriverId`, `AssetId`, `EventStatus`, `ReviewStatus`, `CoachingId`, and `EpochTimeOfEvent`, indicating a complex querying strategy for multi-tenancy and time-series data. Corresponding `SAFETY_EVENT_DB_COL_...` constants define column names.
*   **Event Types and Subtypes**: Categorizes various safety incidents (`SAFETY_EVENT_SUBTYPES` such as "seat-belt-notbuckled", "high-speed", "forward-collision-warning-active") and vehicle operational events (`VEHICLE_EVENT_SUBTYPES` like "vehicle-idle-start", "live-tampering"), which are combined into a comprehensive `EVENT_SUBTYPES` list.
*   **API Endpoints and Parameters**: Specifies resource paths for interacting with the safety event API (e.g., `/safety/asset`, `/safety/{eventId}`, `/safety/{eventId}/updateCoachingInfo`) and API parameters (`API_PARAM_COACHING_ID`).
*   **Microservice Architecture Components**:
    *   **AWS Lambda Functions**: A substantial portion of the file is dedicated to defining constants for numerous AWS Lambda functions, including their names (`_LAMBDA_NAME`), script paths (`_LAMBDA_SCRIPT`), and associated permissions (`_LAMBDA_PERMISSION`). Examples include `safetyLookupDriverHandler`, `safetyReviewHandler`, `safetyPointsHandler`, `safetyEventApiHandler`, and `safetyEventPublishMetricLambdaHandler`.
    *   **Event-Driven Communication**: Extensive use of constants for AWS EventBridge, including event buses (`EVENT_BUS`, `EVENT_BUS_CRON`) and specific event names for publishing and listening (`REVIEW_EVENT_PUBLISH`, `SAFETY_EVENT_UPDATED`, `REVIEW_EVENT_LISTEN`, `UPDATE_EVENT_LISTEN`, `SHARE_EVENT_PUBLISH`, `SAFETY_EVENT_METRICS`).
    *   **State Machine**: Mentions `SAFETY_STATE_MACHINE_NAME` and `STATE_MACHINE_TIMEOUT`, indicating use of AWS Step Functions for orchestrating workflows.
*   **Workflow Statuses**: Defines various states for event reviews (`REVIEW_NEW`, `REVIEW_REQUESTED`, `REVIEW_PERFORMED`, `REVIEW_NOT_PERFORMED`).
*   **Error Handling**: Provides a set of standardized error messages (`ERROR_UNSUPPORTED_OPERATION`, `ERROR_BACKEND_FAILURE`, etc.).
*   **Filtering and Querying**: A comprehensive set of `SAFETY_EVENT_DB_FILTER_...` constants represents common filter expressions used for DynamoDB queries based on attributes like `driverId`, `coachingId`, `assetId`, `tenantId`, `divisionId`, `eventStatus`, `reviewStatus`, and `timeOfEvent`.
*   **Timeouts and Durations**: Configures operational timeouts like `REVIEW_TIMEOUT`, `BRIEF_WAIT`, and `STATE_MACHINE_TIMEOUT` using `aws-cdk-lib`'s Duration.

**Patterns and Recurring Elements**:

*   **Consistent Naming Conventions**: Almost all constants related to this microservice are prefixed with "SAFETY_", and components like database indexes, columns, filters, and Lambda definitions follow clear patterns (e.g., `SAFETY_EVENT_DB_INDEX_...`, `SAFETY_EVENT_DB_COL_...`, `_LAMBDA_NAME`, `_LAMBDA_SCRIPT`, `_LAMBDA_PERMISSION`).
*   **Lambda Triad**: A strong pattern observed for AWS Lambda functions, where each Lambda typically has a dedicated `_LAMBDA_NAME`, `_LAMBDA_SCRIPT` (pointing to an `index.ts` file within a `lambda/` subdirectory), and `_LAMBDA_PERMISSION`.
*   **Identifier Prefixes**: Uses specific prefixes (e.g., `T#`, `D#`, `C#`, `A#`) for various identifiers. Notably, `D#` is used for both `PREFIX_DIVISION_ID` and `PREFIX_DRIVER_ID`, which could be a source of ambiguity or intended for different contexts.
*   **Event-Driven Architecture**: The frequent appearance of "Event" in constant names, along with `PUBLISH` and `LISTEN` actions, and references to `EventBus`, strongly indicates an event-driven design utilizing AWS EventBridge.
*   **Multi-tenancy and Granular Querying**: The large number of DynamoDB indexes, particularly those combining `TenantId` with other attributes and `epochTimeOfEvent`, highlights the importance of supporting multi-tenancy and efficient time-based queries across various dimensions.