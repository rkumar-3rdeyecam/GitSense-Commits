# Activity Summary for 11/8/2025

## 9:04:02 AM
The `c:\ARK-Workspace\MS\microservice-safety\lib\consts.ts` file, last modified on 11/3/2025 at 4:26:00 PM, serves as a central repository for constants used across a "safety" microservice.

**File-specific updates and content:**

*   **Core Data Model:** Defines key parameters for a DynamoDB table named `SafetyEvent`, including its partition key (`tenantIdDivisionId`) and sort key (`correlationId`), along with a global secondary index (`assetGSI`).
*   **Event Definitions:** A comprehensive list of `SAFETY_EVENT_SUBTYPES` (e.g., `seat-belt-notbuckled`, `high-acceleration`, `forward-collision-warning-active`) and `VEHICLE_EVENT_SUBTYPES` (e.g., `vehicle-idle-start`, `live-tampering`), which are combined into `EVENT_SUBTYPES`. This indicates a detailed categorization of events tracked by the service.
*   **Identifiers and Prefixes:** Standardized prefixes (`PREFIX_DIVISION_ID`, `PREFIX_TENANT_ID`, `PREFIX_ASSET_ID`, etc.) are defined, likely for constructing unique identifiers or compound keys.
*   **API Resources:** Specifies API endpoint paths for interacting with safety events, drivers, and assets (e.g., `/safety/asset`, `/safety/{eventId}/updateCoachingInfo`).
*   **Error Handling:** A set of `ERROR_*` constants are present for consistent error messaging across the service.
*   **AWS Lambda Functions:** Numerous constants delineate names, script paths, and permissions for a wide array of Lambda functions, each dedicated to a specific task within the safety domain. Examples include `safetyLookupDriverHandler`, `safetyReviewHandler`, `safetyPointsHandler`, `safetyEventApiHandler`, `safetyEventShareHandler`, and `safetyEventPublishMetricLambdaHandler`, among others. This points to a highly modular and serverless architecture.
*   **Event-Driven Architecture:** Constants for EventBridge bus names (`ArkEventBus`, `default`) and various event types (`REVIEW_EVENT_PUBLISH`, `SAFETY_EVENT_UPDATED`, `SAFETY_EVENT_SCORE_UPDATED`, `SAFETY_EVENT_PUBLISHED`, `UPDATE_EVENT_LISTEN`, `SHARE_EVENT_PUBLISH`, `SAFETY_EVENT_METRICS`) are defined, indicating a strong reliance on event-driven communication between services.
*   **Review Statuses:** Specific statuses for safety event reviews (`REVIEW_NEW`, `REVIEW_REQUESTED`, `REVIEW_PERFORMED`, `REVIEW_NOT_PERFORMED`) are defined.
*   **State Machine:** References a `safetyStateMachine`, suggesting the use of AWS Step Functions for orchestrating complex workflows.
*   **DynamoDB Indexes and Filters:** An extensive collection of `SAFETY_EVENT_DB_INDEX_*` constants reveals a complex data access pattern, with numerous global and local secondary indexes to support efficient querying by various attributes (driver ID, asset ID, tenant ID, event status, epoch time, coaching ID, review status, etc.). Corresponding `SAFETY_EVENT_DB_COL_*` and `SAFETY_EVENT_DB_FILTER_*` constants define column names and filter expressions for these queries.
*   **Timeouts:** Duration constants for review, brief waits, and state machine execution are defined using `aws-cdk-lib.Duration`.

**Patterns and recurring elements:**

*   **Domain-Specific Naming:** Almost all constants are prefixed with "SAFETY", underscoring their direct relation to the safety microservice.
*   **Modularity and Serverless:** The large number of distinct Lambda function definitions (`*_LAMBDA_NAME`, `*_LAMBDA_SCRIPT`, `*_LAMBDA_PERMISSION`) is a strong pattern, indicating a highly modular, function-as-a-service architecture built on AWS Lambda.
*   **Event-Driven Communication:** The extensive set of EventBridge related constants and event types (`publish`, `listen`) signifies an event-driven design for inter-service communication and workflow management.
*   **Complex Data Access:** The numerous DynamoDB indexes and filter expressions highlight the need for flexible and efficient querying of safety events based on a multitude of attributes, suggesting a complex underlying data model and varied reporting/analysis requirements.
*   **AWS Ecosystem Integration:** The import of `aws-cdk-lib`, along with constants for DynamoDB, Lambda, and EventBridge, clearly shows a deep integration with the AWS cloud platform.