# Activity Summary for 11/6/2025

## 5:02:38 PM
The file `c:\ARK-Workspace\MS\microservice-safety\lib\consts.ts`, last updated on `11/3/2025, 4:26:00 PM`, serves as a centralized configuration hub for a "Safety" microservice, defining a vast array of constants used across the application.

Key updates and patterns observed include:

*   **Core Database Configuration:** It establishes fundamental DynamoDB settings like `SAFETY_PARTITION_KEY` ("tenantIdDivisionId"), `SAFETY_SORT_KEY` ("correlationId"), and the main table name `TABLE_SAFETY` ("SafetyEvent"). An extensive list of Global Secondary Indexes (`SAFETY_EVENT_DB_INDEX_*`) and table column names (`SAFETY_EVENT_DB_COL_*`) is defined, indicating a complex data model with diverse query requirements. Numerous `SAFETY_EVENT_DB_FILTER_*` constants specify various filter expressions for querying the database.
*   **Event Definitions:** A comprehensive collection of `SAFETY_EVENT_SUBTYPES` (e.g., "seat-belt-notbuckled", "hard-brake", "forward-collision-warning-active") and `VEHICLE_EVENT_SUBTYPES` (e.g., "vehicle-idle-start", "live-tampering") are listed, which are combined into `EVENT_SUBTYPES`. This highlights the detailed granularity of events processed by the service.
*   **Identifier Prefixes:** Constants like `PREFIX_DIVISION_ID`, `PREFIX_TENANT_ID`, `PREFIX_ASSET_ID`, `PREFIX_DRIVER_ID`, and `PREFIX_COACHING_ID` suggest a structured approach to generating unique identifiers for various entities.
*   **API Endpoints:** REST API resources are clearly defined with constants such as `SAFETY_EVENT_ASSET`, `SAFETY_EVENT_DRIVER`, and `SAFETY_EVENT_COACHING`, outlining the service's external interfaces.
*   **Lambda Function Configuration:** A dominant pattern is the declaration of constants for numerous AWS Lambda functions, each typically including a `_LAMBDA_NAME`, `_LAMBDA_SCRIPT` path, and `_LAMBDA_PERMISSION`. This indicates a heavily serverless architecture, with distinct Lambdas handling tasks like `DRIVER_LOOKUP`, `REVIEW`, `POINTS`, `SAFETY_EVENT`, `SAFETY_OPENSEARCH_PUBLISH`, `SAFETY_INFRACTION_SCORE_CONFIG`, `SAFETY_UPDATE`, `SAFETY_EVENT_API`, `UPDATE_API`, `SAFETY_EVENT_SHARE`, and `SAFETY_EVENT_PUBLISH_METRICS`.
*   **Event-Driven Communication:** The definition of `EVENT_BUS` ("ArkEventBus") and various event publish/listen constants (`REVIEW_EVENT_PUBLISH`, `SAFETY_EVENT_UPDATED`, `UPDATE_EVENT_LISTEN`, `SHARE_EVENT_PUBLISH`) points to a robust event-driven architecture utilizing AWS EventBridge.
*   **Workflow Statuses and Timeouts:** `REVIEW_TIMEOUT`, `BRIEF_WAIT`, `STATE_MACHINE_TIMEOUT` define processing durations, while `REVIEW_NEW`, `REVIEW_REQUESTED`, `REVIEW_PERFORMED`, `REVIEW_NOT_PERFORMED` detail the lifecycle statuses for safety event reviews.
*   **Error Handling:** Specific error messages (`ERROR_UNSUPPORTED_OPERATION`, `ERROR_BACKEND_FAILURE`, etc.) are standardized as constants for consistent logging and response.

The extensive use of constants throughout this file, nearly all prefixed with "SAFETY", underscores its role as a core configuration for a "Safety" microservice. The timestamp of `11/3/2025, 4:26:00 PM` indicates a detailed and potentially significant configuration update, reflecting a mature and well-defined system composed of multiple serverless functions, a DynamoDB backend, and event-driven communication.

## 10:11:11 PM
The file `c:\ARK-Workspace\MS\microservice-safety\lib\consts.ts` was updated on 11/3/2025 at 4:26:00 PM.

This file serves as a central repository for constants within a `microservice-safety` application, largely defining its architecture and data model.

Key updates and patterns include:

*   **DynamoDB Configuration:** Extensive definitions for a `SafetyEvent` DynamoDB table, including primary keys (`SAFETY_PARTITION_KEY`, `SAFETY_SORT_KEY`), over 20 global secondary indexes (`SAFETY_EVENT_DB_INDEX_*`), column names (`SAFETY_EVENT_DB_COL_*`), and more than 15 detailed filter expressions (`SAFETY_EVENT_DB_FILTER_*`). Many indexes combine `TenantId` with other attributes like `DriverId`, `AssetId`, `EventStatus`, and `EpochTimeOfEvent`, indicating support for multi-tenancy and time-series querying.
*   **Event Subtypes:** A comprehensive list of approximately 20 `SAFETY_EVENT_SUBTYPES` (e.g., "seat-belt-notbuckled", "high-speed", "forward-collision-warning") and 4 `VEHICLE_EVENT_SUBTYPES` are defined, which are combined into a master `EVENT_SUBTYPES` list.
*   **AWS Lambda Functions:** Numerous constants define names, script paths, and permissions for a wide array of Lambda functions, highlighting a microservice architecture. These include functions for driver lookup, safety event review, review timeouts, error handling, points calculation, API handling (multiple variants like `safetyEventApi`, `updateApi`, `safetyEventShare`), OpenSearch publishing, infraction score configuration, and metrics publishing.
*   **Event-Driven Architecture:** The application heavily utilizes AWS EventBridge, with constants for event bus names (`EVENT_BUS`, `EVENT_BUS_CRON`) and various event types for publishing and listening (`SAFETY_EVENT_UPDATED`, `REVIEW_EVENT_PUBLISH`, `UPDATE_EVENT_LISTEN`, `SHARE_EVENT_PUBLISH`, `SAFETY_EVENT_METRICS`). A specific comment notes an update (`ARK-4010`) for "unified AI messaging" related to safety event updates.
*   **API Resources:** Specific API endpoints (`/safety/asset`, `/safety/driver`, `/safety/{eventId}`) and parameters are defined, indicating interaction with external or internal clients.
*   **Workflow Statuses:** Defines distinct states for event reviews, such as `REVIEW_NEW`, `REVIEW_REQUESTED`, `REVIEW_PERFORMED`, and `REVIEW_NOT_PERFORMED`.
*   **Error Messages:** A standardized set of error messages (`ERROR_UNSUPPORTED_OPERATION`, `ERROR_BACKEND_FAILURE`, etc.) is provided.
*   **Identifier Prefixes:** Constants like `PREFIX_TENANT_ID`, `PREFIX_DIVISION_ID`, `PREFIX_ASSET_ID`, and `PREFIX_DRIVER_ID` are used for constructing structured identifiers, likely for composite keys or data segmentation.
*   **Timeouts:** Durations for review processes, brief waits, and state machine executions are specified.
*   **AWS Services Integration:** The constants reveal deep integration with AWS services like DynamoDB, Lambda, EventBridge, Step Functions (`SAFETY_STATE_MACHINE_NAME`), and OpenSearch.

Recurring patterns include a consistent naming convention for constants (e.g., `SERVICE_CONCEPT_DETAIL`), the extensive use of Lambda functions for distinct operational concerns, and a highly structured approach to managing data interactions and event-driven communication within the safety microservice.