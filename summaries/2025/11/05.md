# Activity Summary for 11/5/2025

## 2:16:16 PM
The file `c:\ARK-Workspace\MS\microservice-safety\lib\consts.ts` was updated on 11/3/2025, 4:26:00 PM.

This file serves as a comprehensive central repository for constants within a `microservice-safety` application, likely leveraging AWS CDK. Key updates and definitions include:

*   **DynamoDB Configuration:** Extensive definitions for a "SafetyEvent" DynamoDB table, specifying partition and sort keys (`tenantIdDivisionId`, `correlationId`), a Global Secondary Index (`assetGSI`), numerous other DynamoDB indexes (e.g., `SafetyEventIndex-DriverId`, `SafetyEventIndex-TenantId-EventStatus-epochTimeOfEvent`, `SafetyEventIndex-AssetId-TenantId-epochTimeOfEvent`), column names, and complex filter expressions for querying safety events.
*   **Event Management:** A detailed list of `SAFETY_EVENT_SUBTYPES` (e.g., "seat-belt-notbuckled", "high-speed", "forward-collision-warning-active") and `VEHICLE_EVENT_SUBTYPES` ("vehicle-idle-start", "live-tampering").
*   **API Resources:** Definitions for various REST API endpoints related to safety events, such as `/safety/asset`, `/safety/driver`, `/safety/{eventId}`, and `/safety/{eventId}/updateCoachingInfo`.
*   **AWS Lambda Functions:** Constants for over 15 distinct AWS Lambda functions, including their names (e.g., `safetyLookupDriverHandler`, `safetyReviewHandler`, `safetyPointsHandler`, `safetyEventApiHandler`, `safetyEventShareHandler`, `safetyEventPublishMetricLambdaHandler`), script paths, and associated IAM permissions, indicating a highly modular serverless architecture.
*   **Event-Driven Architecture:** Integration with EventBridge is highlighted through constants for event bus names (`ArkEventBus`, `default`) and numerous event types (e.g., `com.ark.review.requested`, `safetyEventUpdated`, `safetyEventScoreUpdated`, `eventSharePublish`, `eventMetricsPublish`), signaling an event-driven design.
*   **Timeouts and Statuses:** Definitions for operation durations (`REVIEW_TIMEOUT`, `STATE_MACHINE_TIMEOUT`), review statuses ("New", "Requested", "Performed", "NotPerformed"), and various error messages.
*   **Identifier Prefixes:** A consistent pattern of prefixes for different entities (e.g., `PREFIX_DIVISION_ID`, `PREFIX_TENANT_ID`, `PREFIX_ASSET_ID`) is used for data organization.

Recurring patterns throughout the file include the extensive use of descriptive prefixes (e.g., `SAFETY_`, `LAMBDA_`, `ERROR_`, `SAFETY_EVENT_DB_INDEX_`, `SAFETY_EVENT_DB_COL_`, `SAFETY_EVENT_DB_FILTER_`) to categorize constants, reflecting a highly structured and organized codebase focused on managing and processing safety-related events within a microservice environment.