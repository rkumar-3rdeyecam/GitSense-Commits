# Activity Summary for 11/5/2025

## 2:16:16 PM
The file `c:\ARK-Workspace\MS\microservice-safety\lib\consts.ts` was updated on 11/3/2025, 4:26:00 PM.

This file serves as a comprehensive central repository for constants within a `microservice-safety` application, likely leveraging AWS CDK. Key updates and definitions include:

*   **DynamoDB Configuration:** Extensive definitions for a "SafetyEvent" DynamoDB table, specifying partition and sort keys (`tenantIdDivisionId`, `correlationId`), a Global Secondary Index (`assetGSI`), numerous other DynamoDB indexes (e.g., `SafetyEventIndex-DriverId`, `SafetyEventIndex-TenantId-EventStatus-epochTimeOfEvent`, `SafetyEventIndex-AssetId-TenantId-epochTimeOfEvent`), column names, and complex filter expressions for querying safety events.
*   **Event Management:** A detailed list of `SAFETY_EVENT_SUBTYPES` (e.g., "seat-belt-notbuckled", "high-speed", "forward-collision-warning-active") and `VEHICLE_EVENT_SUBTYPES` ("vehicle-idle-start", "live-tampering").
*   **API Resources:** Definitions for various REST API endpoints related to safety events, such as `/safety/asset`, `/safety/driver`, `/safety/{eventId}`, and `/safety/{eventId}/updateCoachingInfo`.
*   **AWS Lambda Functions:** Constants for over 15 distinct AWS Lambda functions, including their names (e.g., `safetyLookupDriverHandler`, `safetyReviewHandler`, `safetyPointsHandler`, `safetyEventApiHandler`, `safetyEventShareHandler`, `safetyEventPublishMetricLambdaHandler`), script paths, and associated IAM permissions, indicating a highly modular serverless architecture.
*   **Event-Driven Architecture:** Integration with EventBridge is highlighted through constants for event bus names (`ArkEventBus`, `default`) and numerous event types (e.g., `com.ark.review.requested`, `safetyEventUpdated`, `safetyEventScoreUpdated`, `eventSharePublish`, `eventMetricsPublish`), signaling an event-driven design.
*   **Timeouts and Statuses:** Definitions for operation durations (`REVIEW_TIMEOUT`, `STATE_MACHINE_TIMEOUT`), review statuses ("New", "Requested", "Performed", "NotPerformed"), and various error messages.
*   **Identifier Prefixes:** A consistent pattern of prefixes for different entities (e.g., `PREFIX_DIVISION_ID`, `PREFIX_TENANT_ID`, `PREFIX_ASSET_ID`) is used for data organization.

Recurring patterns throughout the file include the extensive use of descriptive prefixes (e.g., `SAFETY_`, `LAMBDA_`, `ERROR_`, `SAFETY_EVENT_DB_INDEX_`, `SAFETY_EVENT_DB_COL_`, `SAFETY_EVENT_DB_FILTER_`) to categorize constants, reflecting a highly structured and organized codebase focused on managing and processing safety-related events within a microservice environment.

## 7:00:07 PM
The file `c:\ARK-Workspace\MS\microservice-safety\lib\consts.ts` was updated on November 3, 2025, at 4:26:00 PM. This file serves as a central repository for a vast array of constants used across a microservice application focused on safety events.

**File-Specific Updates:**
The update to `consts.ts` introduced or modified numerous constants categorised for various aspects of the "microservice-safety" application:

*   **AWS DynamoDB Configuration:** Defines core table names (`SafetyEvent`), partition/sort keys (`tenantIdDivisionId`, `correlationId`), Global Secondary Index (GSI) names (`assetGSI`), and an extensive list of DynamoDB indexes (e.g., `SafetyEventIndex-DriverId`, `SafetyEventIndex-TenantId-epochTimeOfEvent`, `SafetyEventIndex-TenantId-ReviewStatus-epochTimeOfEvent`) and column names (e.g., `driverId`, `assetId`, `epochTimeOfEvent`, `tenantIdDivisionId`, `videoRequestId`). Many of these reflect composite keys for efficient querying.
*   **Event Definitions:** Declares a comprehensive set of safety event subtypes (e.g., "seat-belt-notbuckled", "high-speed", "forward-collision-warning-active") and vehicle event subtypes ("vehicle-idle-start", "live-tampering"), which are combined into a master `EVENT_SUBTYPES` list.
*   **System Identifiers and Prefixes:** Establishes prefixes like `T#` for Tenant ID, `D#` for Division ID and Driver ID, `C#` for Coaching ID, `A#` for Asset ID, and various others for event and review statuses.
*   **API Resources:** Specifies API paths such as `/safety/asset`, `/safety/driver`, `/safety/{eventId}`, and `/safety/{eventId}/updateCoachingInfo`, indicating a RESTful interface for safety events.
*   **Error Messages:** A collection of standardised error messages, all beginning with "🛑Error ⇨".
*   **AWS Lambda Function Definitions:** Defines names, script paths, and permissions for approximately 15 distinct Lambda functions, including handlers for driver lookup, event review, review timeouts, error handling, points calculation, state machines, OpenSearch publishing, infraction score configuration, status updates, API interactions, sharing, and metrics publishing.
*   **Application State and Workflow:** Includes constants for review timeouts, brief waits, state machine timeouts, and distinct review statuses (`New`, `Requested`, `Performed`, `NotPerformed`).
*   **EventBridge Events:** Defines various event names for publishing and listening on the `ArkEventBus` and `default` cron bus, such as `com.ark.review.requested`, `safetyEventUpdated`, `safetyEventScoreUpdated`, `safetyReviewComplete`, `eventSharePublish`, and `eventMetricsPublish`.
*   **Utility Constants:** Includes `GEOSEARCH_RADIUS_METERS` and `ALL_ATTRIBUTES`, along with API parameters for filtering.

**Patterns and Recurring Elements:**

*   **Consistent Naming Conventions:** A strong pattern of `SERVICE_COMPONENT_TYPE` (e.g., `SAFETY_PARTITION_KEY`, `REVIEW_TIMEOUT`, `ERROR_UNSUPPORTED_OPERATION`) is used throughout. Lambda-related constants consistently follow `LAMBDA_NAME`, `LAMBDA_SCRIPT`, and `LAMBDA_PERMISSION` patterns for each function. DynamoDB indexes and columns also follow `SAFETY_EVENT_DB_INDEX_...` and `SAFETY_EVENT_DB_COL_...` respectively.
*   **AWS Service Focus:** The constants heavily leverage AWS services, particularly DynamoDB (extensive indexing and column definitions), AWS CDK (for `Duration`), AWS Lambda (numerous function definitions), and AWS EventBridge (for event buses and specific event messages). This indicates a serverless, event-driven architecture.
*   **Safety Event Centricity:** The vast majority of constants revolve around "safety events," their attributes, statuses, related entities (drivers, assets, tenants, coaching), and the various processing steps and workflows involved.
*   **Modular Architecture:** The clear separation of concerns, especially visible in the distinct Lambda functions for specific tasks (lookup, review, points, share, metrics, updates), suggests a well-defined microservice approach.
*   **Data Granularity and Query Needs:** The detailed DynamoDB indexes and composite column names imply complex and varied data retrieval requirements based on combinations of `tenantId`, `divisionId`, `assetId`, `driverId`, `eventStatus`, `reviewStatus`, and `epochTimeOfEvent`.
*   **Script Path Structure:** Most Lambda script paths follow the `lambda/<function_name_folder>/index.ts` convention, simplifying the location of Lambda code.
*   **Standardised Error Messages:** All error messages adhere to the "🛑Error ⇨ " prefix, making them easily identifiable in logs.